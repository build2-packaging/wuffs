: run
:
# Upstream tests chdir up until they see wuffs-root-directory.txt,
# then read test/data/ relative to that directory. Start in the
# git submodule, which is not part of the published archive.
#
env -c $src_root/../../upstream -- $* >!
