./: {*/ -build/ -unit/ -test/ -release/}

# Extra POSIX tests. Not on MSVC (unistd.h). Nested under unit/ so
# ../../../release/c/ from std/ stays inside this subproject.
# Keep them on ./: so dist ships the sources.
#
./: unit/c/: include = ($cxx.target.system != 'win32-msvc')
